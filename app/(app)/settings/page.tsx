import { ComingSoon } from "@/components/ComingSoon";

export default function SettingsPage() {
  return (
    <ComingSoon
      title="Einstellungen"
      emoji="⚙️"
      description="Habits-CRUD, Theme, Erinnerungszeiten, Datenexport — kommt in Etappe 2c."
    />
  );
}
